# Step 1 - Repo Cleanup and Archive (Completed)

**Goal:** Remove skill artifacts from the root and keep them in `.archive/skill/` for reference.

**Work:**
- Move `SKILL.md` and `scripts/` into `.archive/skill/`
- Ensure `.archive/` is tracked in git

**Done When:**
- Root no longer contains `SKILL.md` or `scripts/`
- Files exist under `.archive/skill/`

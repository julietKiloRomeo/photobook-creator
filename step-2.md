# Step 2 - Minimal UI Shell (Drag + Drop) (Completed)

**Goal:** A local web UI that launches and accepts image drag/drop.

**Work:**
- Create a minimal web app (React/Vite)
- Implement drag/drop surface
- Show a list of dropped image filenames (no processing yet)

**Done When:**
- App runs locally
- Dragging image files into the UI lists them
